langchain>=0.3.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.0
httpx>=0.27.0

# Optional — only needed to push events via Google Calendar API
# (ICS files + Google "Add event" links work without these):
# google-api-python-client>=2.0.0
# google-auth-oauthlib>=1.0.0
# google-auth-httplib2>=0.2.0
